fastapi==0.115.0
uvicorn[standard]==0.32.0
pytest==8.2.0
pytest-asyncio==0.23.7
httpx==0.27.0
openai==1.40.0            # openai sdk for LLM client (async stream + tool calls)
sse-starlette==2.1.0      # sse support for FastAPI
tiktoken==0.7.0            # 上下文压缩 token 确定性计数（cl100k_base，非字符估算）
